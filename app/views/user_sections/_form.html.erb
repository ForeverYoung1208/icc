<%= form_for(@user_section) do |f| %>
  <% if @user_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_section.errors.count, "error") %> prohibited this user_section from being saved:</h2>

      <ul>
      <% @user_section.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.collection_select( :user, User.all, :id, :email ) %>
    <%= link_to t('users.new'), new_user_registration_path %>

  </div>
  <div class="field">
    <%= f.label :section %><br />
    <%= f.collection_select( :section, Section.all, :id, :name_int )  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
