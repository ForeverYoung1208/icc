$('#content').hide('fade', function(){
	$('#content').html("<%= escape_javascript(render('index')) %>");
});

$('#content').show('fade',function() {
	stopLoadingAnimation();
	<% if !@show_section.nil? %>
		$.scrollTo('<%= @show_section %>', 500)
	<% end %>
}).on('click', '.up_arrow', function(event){
	$.scrollTo('#logo', 300);	
});;

$('a#newses').addClass('selected',300);